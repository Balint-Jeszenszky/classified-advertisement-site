import { MultipleLineTextPipe } from './multiple-line-text.pipe';

describe('MultipleLineTextPipe', () => {
  it('create an instance', () => {
    const pipe = new MultipleLineTextPipe();
    expect(pipe).toBeTruthy();
  });
});
